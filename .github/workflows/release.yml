name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Build
        run: |
          mkdir -p release

          # Linux
          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o release/cmon-${{ github.ref_name }}-linux-amd64 .
          GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o release/cmon-${{ github.ref_name }}-linux-arm64 .

          # macOS
          GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o release/cmon-${{ github.ref_name }}-darwin-amd64 .
          GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o release/cmon-${{ github.ref_name }}-darwin-arm64 .

          # Windows
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o release/cmon-${{ github.ref_name }}-windows-amd64.exe .
          GOOS=windows GOARCH=arm64 go build -ldflags="-s -w" -o release/cmon-${{ github.ref_name }}-windows-arm64.exe .

      - name: Publish
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: release/*
